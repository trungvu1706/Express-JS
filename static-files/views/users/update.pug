extends ../layouts/common 
block content

    h1 UPDATE PROFILE 

    form(action="/users/" + user.id + "/update", method="POST")
        label User Name
        input(type="text", name="name")
        span &nbsp
        button Create